# TODO Project Title

> One-sentence orientation that frames what the rest of the site argues. Becomes the lede paragraph.

**TL;DR.** One-paragraph executive summary covering the whole site. This paragraph becomes a styled TLDR block automatically. Keep it tight — three to five sentences. Status of the project at this moment, the load-bearing claim, and what the reader will find on each page.

## How to read

| Page | Audience | Status |
|---|---|---|
| **[Page A](page-a.html)**: short headline | Who reads this page | One-line summary of current state |
| **[Page B](page-b.html)**: short headline | Who reads this page | One-line summary |

This markdown table gets replaced with the card grid defined in `render.py` (`INDEX_CARDS_HTML`). Keep the table here — it stays as the editable source-of-truth; readers downloading the .md see structured rows.

## Status legend used throughout

- `PASS`: confirmed against evidence, locked
- `WARN`: in-flight, expected to evolve
- `OPEN`: undecided or under investigation
- `FAIL`: blocked or contradicted
- `LOCKED`: non-negotiable production constraint

## TODO sections

Anything you want on the landing page beyond the table can go here. Index pages are typically short — the card grid does most of the orientation work.

---

**Drafted YYYY-MM-DD.** Provenance notes (transcripts, decisions, prior docs) belong here.
